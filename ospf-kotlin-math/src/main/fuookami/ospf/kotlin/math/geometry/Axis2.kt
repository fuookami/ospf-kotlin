/**
 * 二维坐标轴枚举
 * 2D Axis Enum
 *
 * 定义二维几何空间中的坐标轴枚举（X、Y），为纯几何概念，可跨域迁移。
 * Defines coordinate axis enum (X, Y) in 2D geometric space, a pure geometry concept migratable across domains.
*/
package fuookami.ospf.kotlin.math.geometry

/**
 * 二维坐标轴纯几何枚举，可跨域迁移。
 * Pure 2D axis enum, migratable across domains.
*/
enum class Axis2 {
    X,
    Y
}
