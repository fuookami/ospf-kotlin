/**
 * 三维坐标轴枚举
 * 3D Axis Enum
 *
 * 定义三维几何空间中的坐标轴枚举（X、Y、Z），为纯几何概念，可跨域迁移。
 * Defines coordinate axis enum (X, Y, Z) in 3D geometric space, a pure geometry concept migratable across domains.
 */
package fuookami.ospf.kotlin.math.geometry

/**
 * 三维坐标轴纯几何枚举，可跨域迁移。
 * Pure 3D axis enum, migratable across domains.
 */
enum class Axis3 {
    X,
    Y,
    Z
}
