<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>io.github.fuookami.ospf.kotlin</groupId>
        <artifactId>ospf-kotlin-starters</artifactId>
        <version>1.1.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <name>ospf-kotlin-starter-gantt-scheduling</name>
    <artifactId>ospf-kotlin-starter-gantt-scheduling${ospf.kotlin.artifact.suffix}</artifactId>

    <dependencies>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin</groupId>
            <artifactId>ospf-kotlin-starter${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-infrastructure${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-domain-task-context${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-domain-task-generation-context${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-domain-task-compilation-context${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-domain-bunch-generation-context${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-domain-bunch-compilation-context${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-domain-capacity-scheduling-context${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-domain-produce-context${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-domain-resource-context${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuookami.ospf.kotlin.framework.gantt-scheduling</groupId>
            <artifactId>ospf-kotlin-framework-gantt-scheduling-application${ospf.kotlin.artifact.suffix}</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>
