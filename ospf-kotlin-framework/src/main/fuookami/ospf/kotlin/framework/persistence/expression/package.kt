/**
 * 表达式模块
 * Expression Module
 *
 * 提供 SQL 表达式的框架层实现，包括字段映射、排序、更新赋值和 Ktorm 翻译器。
 * Provides framework-level implementation for SQL expressions, including field mapping,
 * sorting, update assignments, and Ktorm translators.
 */
package fuookami.ospf.kotlin.framework.persistence.expression