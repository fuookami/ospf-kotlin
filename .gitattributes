# Auto detect text files and perform LF normalization
* text=auto

# 跨语言契约 fixture 必须逐字节确定：固定 LF，避免 Windows 检出把 LF 改成 CRLF。
# Cross-language contract fixtures must be byte-deterministic: pin LF so a Windows
# checkout does not rewrite LF as CRLF.
*.tsv text eol=lf
