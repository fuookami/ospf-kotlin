package fuookami.ospf.kotlin.utils.math.ordinary

import fuookami.ospf.kotlin.utils.math.FloatingNumber
import fuookami.ospf.kotlin.utils.math.FloatingNumberConstants

fun <T : FloatingNumber<T>> log(x: T, base: T, constants: FloatingNumberConstants<*>): T {
    // todo: use taylor formula to replace it
    return x
}
